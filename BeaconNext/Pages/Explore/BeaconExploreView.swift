import SwiftUI

struct BeaconExploreView: View {
    var body: some View {
        VStack {
            
        }
        .navigationTitle("Explore")
        .navigationBarTitleDisplayMode(.large)
    }
}

#Preview {
    BeaconExploreView()
}
