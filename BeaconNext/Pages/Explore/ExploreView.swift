import SwiftUI

struct ExploreView: View {
    var body: some View {
        VStack {
            
        }
        .navigationTitle("Explore")
        .navigationBarTitleDisplayMode(.large)
    }
}

#Preview {
    ExploreView()
}
