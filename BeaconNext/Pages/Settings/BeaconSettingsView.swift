import SwiftUI

struct BeaconSettingsView: View {
    var body: some View {
        VStack {
            
        }
        .navigationTitle("Settings")
        .navigationBarTitleDisplayMode(.large)
    }
}

#Preview {
    BeaconSettingsView()
}
