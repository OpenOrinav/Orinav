import SwiftUI

struct SettingsView: View {
    var body: some View {
        VStack {
            
        }
        .navigationTitle("Settings")
        .navigationBarTitleDisplayMode(.large)
    }
}

#Preview {
    SettingsView()
}
